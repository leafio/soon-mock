{
  "name": "soon-mock",
  "version": "0.0.1-beta.11",
  "description": "a mock server can add apis in seconds with web UI",
  "license": "MIT",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "bin": "./bin/index.js",
  "keywords": [
      "mock",
      "server",
      "api",
      "express",
      "ui"
  ],
  "devDependencies": {
    "vitepress": "^1.3.0"
  },
  "dependencies": {
      "commander": "^12.1.0"
  }
}
